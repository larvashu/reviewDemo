# Baza Danych (PostgreSQL)
db.host=localhost
db.port=5432
db.name=testdb
db.user=testuser
db.pass=testpass

# Kolejka Wiadomosci (RabbitMQ)
rabbitmq.host=localhost
rabbitmq.port=5672
rabbitmq.user=guest
rabbitmq.pass=guest

# Nazwa kolejki (usywana przez workera)
app.queue.name=order_queue
